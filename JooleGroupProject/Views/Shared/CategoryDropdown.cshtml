@model JooleGroupProject.Models.JooleModel


    <select class="categoryDropdown" id="catSelect" >
        @foreach (var v in Model.SearchView.Categories)
        {
            <option value="@v.Category_ID">@v.Category_Name</option>
        }
    </select>
    

